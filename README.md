# nikbsh.github.io

Personal research profile for Nikolay Bashlykov.

The site is intentionally static: edit `index.html` and `style.css`, commit to
the `main` branch, and GitHub Pages will publish it at
`https://nikbsh.github.io/`.
